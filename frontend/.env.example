# Copiar para .env (local) ou definir na Vercel (Production / Preview)
#
# Local: deixa comentado VITE_DEV_REMOTE e usa o proxy do Vite (localhost:8888)
# Produção na Vercel: URL pública do API (Render, Railway, etc.) — com barra no fim

# VITE_DEV_REMOTE=remote
VITE_BACKEND_SERVER=https://gestpr-api.onrender.com/
VITE_FILE_BASE_URL=https://gestpr-api.onrender.com/
